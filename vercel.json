{
  "installCommand": "pnpm install && cd packages/db && npx prisma generate --schema=./prisma/schema.prisma",
  "buildCommand": "turbo run build --filter=@sigef/web...",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
