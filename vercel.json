{
  "installCommand": "pnpm install",
  "buildCommand": "cd packages/db && npx prisma generate --schema=./prisma/schema.prisma && cd ../.. && turbo run build --filter=@sigef/web...",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
